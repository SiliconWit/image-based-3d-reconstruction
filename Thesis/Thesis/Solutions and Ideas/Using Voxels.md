#path-planning 
Instead of creating sparse point clouds, the space can be discretized into uniform 3D grids or voxels, where each voxel can represent occupancy, color, or other relevant properties.
This is particularly useful for applications where a volumetric representation is preferred.

Some of the methods of representing space include:
	1. Octomap
	2. **Voxelmap Representation**
